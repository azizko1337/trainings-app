import styled from "styled-components";

const ParticipantsListContainer = styled.ul`
  display: flex;
  flex-direction: column;
  list-style: none;

  margin-top: 20px;
`;

export default ParticipantsListContainer;
