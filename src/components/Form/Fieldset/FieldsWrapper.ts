import styled from "styled-components";

const FieldsWrapper = styled.div`
    display: flex;
    padding: 5px 0;
    gap: 20px;

    & > div{
        display: flex;
        gap: 5px;
        align-items: center;
    }
`;

export default FieldsWrapper;